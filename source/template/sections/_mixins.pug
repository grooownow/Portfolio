mixin nav(className, items, hrefs)
	nav(class='nav ' + `${className}`)
		ul(class=`${className}__list`)
			each item, ndx in items
				li(class=`${className}__item`)
					a(class=`${className}__link` href=`${hrefs[ndx]}`)= item

mixin socials(className,socials, hrefs)
	ul(class='c-socials ' + `c-socials_${className}`)
		each social, ndx in socials
			li(class='c-socials__item ' + `c-socials__item_${className}`)
				a(class='c-socials__link ' + `c-socials__link_${social}` href=`${hrefs[ndx]}`, target='_blank')
					svg(class='c-socials__pic ' + `c-socials__pic_${className}`)
						use(xlink:href=`./assets/images/sprite.svg#${social}`)

mixin hero__bg(svgName)
	div(class='.c-hero__bg ' + `c-hero__bg_${svgName}`)

